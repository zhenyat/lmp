<%= form_for([:admin, @department_type]) do |f| %>
  <% if @department_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@department_type.errors.count, "error") %> prohibited this department_type from being saved:</h2>

      <ul>
      <% @department_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
      <%= f.label :name %><br>
    <%= f.text_field :name %>
    </div>
  <div class="field">
      <%= f.label :name %><br>
    <%= f.text_field :name %>
    </div>
  <div class="field">
      <%= f.label :title %><br>
    <%= f.text_field :title %>
    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
