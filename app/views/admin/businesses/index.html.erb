<h1>Listing businesses</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Name</th>
      <th>Title</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @businesses.each do |business| %>
      <tr>
        <td><%= business.name %></td>
        <td><%= business.name %></td>
        <td><%= business.title %></td>
        <td><%= link_to 'Show', [:admin, business] %></td>
        <td><%= link_to 'Edit', edit_admin_business_path(business) %></td>
        <td><%= link_to 'Destroy', [:admin, business], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Business', new_admin_business_path %>
