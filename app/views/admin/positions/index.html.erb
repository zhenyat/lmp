<h1>Listing positions</h1>

<table>
  <thead>
    <tr>
      <th>Business</th>
      <th>Name</th>
      <th>Title</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @positions.each do |position| %>
      <tr>
        <td><%= position.business_id %></td>
        <td><%= position.name %></td>
        <td><%= position.title %></td>
        <td><%= link_to 'Show', [:admin, position] %></td>
        <td><%= link_to 'Edit', edit_admin_position_path(position) %></td>
        <td><%= link_to 'Destroy', [:admin, position], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Position', new_admin_position_path %>
